{
  "name": "genesis-proving-ground",
  "private": true,
  "version": "0.1.0-genesis",
  "type": "module",
  "description": "PROJECT GENESIS (web substitution track) — an original space-academy craft-racing proving ground: one craft, one graybox track, physics, camera, telemetry, sense of speed.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^5.4.8"
  }
}
